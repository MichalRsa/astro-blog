---
import clsx from "clsx";

interface Props {
  bgColor: "bg-secondary-700" | "bg-ternary-200";
}

const { bgColor } = Astro.props;
---

<section class={clsx(bgColor, "p-12")}>
  <div class="mx-auto max-w-7xl">
    <slot />
  </div>
</section>
